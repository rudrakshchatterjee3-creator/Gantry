# Build-time only (baked into the client bundle by `next build`) — not a
# Worker runtime secret. See wrangler.jsonc for why OFFICIAL_INVITE_CODE
# itself lives there instead of the Cloudflare dashboard.
NEXT_PUBLIC_OFFICIAL_INVITE_CODE_HINT=GANTRY2026
